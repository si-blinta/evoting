Problem:
Any one can forge a new commit by taking the sequence number and the signed sequence number and put it on old commit.
Solution: add the sequence number to the commit hash => (C|S|seq)
when verifiying:
check if H(C|S|seq) == seq 
ou peut etre une seule signature qui regroupe tout ! 